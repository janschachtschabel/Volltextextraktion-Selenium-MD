# FastAPI settings
# HOST: Bind-Adresse der API (Default: 0.0.0.0)
HOST=0.0.0.0
# PORT: TCP-Port der API (Default: 8000)
PORT=8000

# LLM settings (optional)
# LLM_BASE_URL: OpenAI-kompatible Base URL (z. B. https://api.openai.com/v1)
LLM_BASE_URL=https://api.openai.com/v1
# LLM_MODEL: z. B. gpt-5-mini, gpt-4o-mini, o. kompatibel
LLM_MODEL=gpt-5-mini
# LLM_API_KEY: API-Schlüssel (oder OPENAI_API_KEY als Fallback)
LLM_API_KEY=
# Alternative: OPENAI_API_KEY= (fallback for Google Colab)

# Defaults for crawling
# DEFAULT_MODE: auto|fast|js (Default: auto)
DEFAULT_MODE=auto
# DEFAULT_JS_STRATEGY: speed|accuracy (Default: speed)
DEFAULT_JS_STRATEGY=speed
# DEFAULT_TIMEOUT_SECONDS: int Sekunden (1-600), Gesamtbudget pro Request
DEFAULT_TIMEOUT_SECONDS=240
# DEFAULT_RETRIES: int (0-10), Wiederholungsversuche bei Netzwerk-/5xx-Fehlern
DEFAULT_RETRIES=2
# DEFAULT_HEADLESS: bool (true|false), Headless-Browser im JS-Modus
DEFAULT_HEADLESS=true
# DEFAULT_STEALTH: bool (true|false), Anti-Detection-Flags aktivieren
DEFAULT_STEALTH=true
# DEFAULT_JS_AUTO_WAIT: bool (true|false), interne Auto-Waits (konservativ)
DEFAULT_JS_AUTO_WAIT=true
# DEFAULT_MAX_BYTES: int Bytes (1024-104857600), harte Obergrenze für Antwortgröße
DEFAULT_MAX_BYTES=10485760
# DEFAULT_USER_AGENT: Browser User-Agent-String für Selenium
DEFAULT_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/136.0.0.0 Safari/537.36

# Selenium Pool Configuration
# SELENIUM_POOL_SIZE: Startgröße des Driver-Pools (int >=1)
SELENIUM_POOL_SIZE=2
# SELENIUM_MAX_POOL_SIZE: Obergrenze beim Auto-Scaling (int >= POOL_SIZE)
SELENIUM_MAX_POOL_SIZE=8
# SELENIUM_SCALE_THRESHOLD: Auslastung für Scale-Up (float 0.0-1.0)
SELENIUM_SCALE_THRESHOLD=0.8

# Request Capacity & Queuing
# MAX_CONCURRENT_REQUESTS: gleichzeitige Verarbeitungen (int >=1)
MAX_CONCURRENT_REQUESTS=8
# MAX_QUEUE_SIZE: maximale Queue-Länge für wartende Requests (int >=0)
MAX_QUEUE_SIZE=50
# QUEUE_TIMEOUT_SECONDS: maximale Wartezeit in der Queue (Sekunden)
QUEUE_TIMEOUT_SECONDS=60

# Media Handling (skip|metadata|full|none)
# MEDIA_CONVERSION_POLICY:
#   skip     → Audio/Video nicht konvertieren (schnell, empfehlenswert)
#   metadata → Nur ffprobe-Metadaten als JSON im Markdown (ffprobe benötigt)
#   full     → Volle Konvertierung via markitdown/ffmpeg (langsam, hoher Ressourcenbedarf)
#   none     → Gar keine Ausgabe für Medien (leerer String)
MEDIA_CONVERSION_POLICY=skip

# HTML Conversion
# HTML_CONVERTER: trafilatura|markitdown|bs4 (Default: trafilatura)
#   trafilatura → Schnelle, robuste Extraktion des Inhaltskerns (Fallback: MarkItDown → BS4)
#   markitdown  → Volle HTML→Markdown-Konvertierung (Fallback: BS4)
#   bs4         → Einfache Text-Extraktion mit BeautifulSoup (nur HTML)
HTML_CONVERTER=trafilatura

# TRAFILATURA_CLEAN_MARKDOWN: bool (true|false)
#   true  → "Bereinigtes" Markdown (Hauptinhalt) via Trafilatura.extract(output_format=markdown)
#   false → "Roh"-Extraktion via Trafilatura.html2txt (Plaintext, markdown-kompatibel)
TRAFILATURA_CLEAN_MARKDOWN=true

# Security
# ALLOW_INSECURE_SSL: bool (true|false)
#   true  → SSL-Zertifikate werden NICHT validiert (httpx verify=false)
#   false → Strikte Zertifikatsprüfung (empfohlen)
ALLOW_INSECURE_SSL=false
